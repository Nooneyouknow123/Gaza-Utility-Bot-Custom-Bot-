# 🚀 Gaza Guild Moderation Bot  
Advanced Moderation • Jail System • Staff Strikes • Logging • Utilities

A fully–featured Discord moderation system built for **Gaza Guild (29k+ members)** with reliability, security, and staff workflow automation in mind.

Developed by:
- **Syed Wajih** — Lead Developer  
- **Kingbossali** — Developer  
Both worked jointly with direct insight from the Lead Developer.

---

# 📌 Features Overview

## 🔧 Moderation Commands
- `.warn` – Issue a warning with tracked reasons  
- `.warnlist` – View all warnings a user has  
- `.removewarn` – Remove a warning by ID  
- `.clearwarns` – Clear *all* warnings for a user  
- `.mute` – Timeout a user for a set duration  
- `.unmute` – Remove a user's timeout  
- `.ban` – Permanently ban a user  
- `.unban` – Unban using user ID  
- `.kick` – Remove a user from the server  
- `.note` – Add private staff notes  
- `.notelist` – Show all notes for a user  
- `.removenote` – Delete a staff note by ID  
- `.whois` – Full user moderation history & info

---

## 🔒 Jail System
A complete automated jail system designed for high-traffic servers.

- `.setupjail` – Auto-creates all jail roles & channels  
- `.setjailadmins` – Assign allowed jail managers  
- `.jail` – Jail a user (optional timed jail)  
- `.unjail` – Restore user roles after jail  
- `.accept` – Approve jail appeal  
- `.deny` – Deny jail appeal  
- `.lockdownjail` – Isolate jailed users  
- `.fixjailperms` – Repair jail permissions across server  

Includes:  
✔ Button-based appeal tickets  
✔ Automatic role restoration  
✔ Anti-abuse permissions  

---

## ⚠ Staff Strike System (Slash Commands)
- `/infract` – Strike a staff member with custom duration  
- `/infract_channel` – Set strike log channel  
- `/strike_history` – View complete strike history  
- `/remove_strike` – Remove the most recent strike  
- `/strike_info` – Check your own strike status  

---

## 📜 Logging System
- `.setlog` – Configure log channels for events  
- `.logconfig` – Show your current logging setup  

Automatic logs include:  
- Moderation actions  
- Message edits/deletes  
- Kicks/bans  
- Boost logs  
- Jail actions  
…and more.

---

## 🧰 Utility Commands
- `.avatar` – Show user avatar + download link  
- `.serverinfo` – Full server stats  
- `.members` – Member count breakdown  
- `.roles` – List all roles with counts  
- `.botinfo` – Bot stats & system info  
- `.ping` – Check latency  
- `.config` – Show full bot configuration  

---

## 🛠 Server Management
- `.setstaffrole` – Define staff roles with mod permissions  
- `.setlogchannel` – Set mod-action log channel  
- `.jailrole` – Set main jail role  
- `.safelist` – Protect roles/users from moderation  

---

## 🧹 Message Management
- `.purge` – Bulk delete messages  
- `.purgeuser` – Delete messages from a specific user  
- `.snipe` – Retrieve last deleted message  
- `.editsnipe` – Show pre/post edit message  

---

## 🔍 Developer Commands
- `.reload` – Hot reload cogs  
- `.list_commands` – Show all commands  
- `/servers` – List servers bot is in  
- `/invite_to` – Generate server-specific bot invite  
- `.addrole` – Give a user a role (authorized only)

---

## 🎭 Fun Commands
- `.fakeban` – Fake ban for pranks  
- `/fakeban` – Slash command version  

---

## 🎉 Boost Tracking
Automatic thank-you embed when users boost the server.

---

## 📨 Appeal System
- Fully button-based  
- Auto-ticket creation  
- Staff accept/deny buttons  
- Logs every action  

---

# 👑 Team
**Syed Wajih** — Lead Developer  
**Kingbossali** — Developer  

A collaborative project with shared effort and leadership from the Lead Developer.

---

# 🌍 Server
**Gaza Guild** — 29,000+ Members  
High-scale moderation bot built specifically for this community.

---

# 📦 License
Private project — all rights reserved.


